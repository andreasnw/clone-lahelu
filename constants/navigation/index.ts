export enum NavigationIndex {
  TABS = "(home,post,profile,topics)",
  NOT_FOUND = "+not-found",
}

export enum BottomTabNavigatorEnum {
  INDEX = "index",
  POST = "post",
  TOPICS = "topics",
  PROFILE = "profile",
  TOP_TABS = "(top-tabs)",
}

export enum DrawerNavigatorEnum {
  HOME = "(home)/(top-tabs)",
  FRESH = "(home)/(top-tabs)/fresh",
  TRENDING = "(home)/(top-tabs)/trending",
}

export enum TopTabNavigatorEnum {
  HOME = "index",
  FRESH = "fresh",
  TRENDING = "trending",
}
